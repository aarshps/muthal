# Muthal — Claude Code quick context

Muthal is a minimal **income & expense ledger for institutions** (temples, churches,
libraries, …) — a Hora-family cross-platform monorepo (Android + iOS + Web + shared
contract). Start with [`AGENTS.md`](AGENTS.md) for the cross-cutting mandates and
[`shared/domain/SPEC.md`](shared/domain/SPEC.md) for the canonical data model.

## Current version

- **versionName:** `1.0-beta.5`  ·  **versionCode:** `5`  ·  **tag:** `v1.0-beta.5`
- **Next free versionCode:** `6`

(Bump per `hora-core/docs/conventions.md` → "App versioning": `versionCode` +1 every build
that reaches any Play track; `versionName` `MAJOR.MINOR-beta.N`. Keep this line current.)

## Identity

- App name: **Muthal** · package / bundle id: `com.hora.muthal`
- Icon wordmark: **മുത** (Malayalam) — generated by the hora-core launcher-icon engine
- Firebase project: **`hora-muthal`** (project number `310482235422`) — Muthal's own,
  never shared with sibling apps. Console: https://console.firebase.google.com/project/hora-muthal

## Golden rules

- Public repo → **no secrets ever** (see `AGENTS.md` mandate 1, `DISASTER_RECOVERY.md`).
- Commit straight to `main` (repo) / `master` (wiki) — no PRs.
- Keep Android/iOS/Web behaviour identical; all three assert `shared/domain/golden-vectors.json`.
- Shared code comes from hora-core via sync scripts — never hand-edit a generated copy.
