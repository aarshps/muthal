Beta 3: the app now uses the Hora family brand font (Google Sans Flex) across every screen, and Google Sign-In errors are reported accurately instead of always showing "cancelled". Sign-in from Play-installed builds is fixed (signing-certificate registration).
