<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MuthalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.App.Starting">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.Muthal" />

        <activity
            android:name=".ui.ManageMembersActivity"
            android:exported="false"
            android:theme="@style/Theme.Muthal" />

        <activity
            android:name=".ui.CategoriesActivity"
            android:exported="false"
            android:theme="@style/Theme.Muthal" />

        <activity
            android:name=".ui.PeriodExportActivity"
            android:exported="false"
            android:theme="@style/Theme.Muthal" />

        <!-- App Links target for the universal join link (SPEC §2). Verified via
             https://muthal-web.vercel.app/.well-known/assetlinks.json, which lists both
             the Play App Signing cert and the upload-key cert (GitHub release APKs are
             signed with the latter, Play-installed apps present the former) — confirmed
             live against Google's Digital Asset Links API. If verification ever regresses
             (e.g. the file goes missing), Android falls back to opening the web join page
             instead of the app, which still works (SPEC's "universal fallback"). -->
        <activity
            android:name=".ui.JoinActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="muthal-web.vercel.app"
                    android:pathPrefix="/join/" />
            </intent-filter>
        </activity>
    </application>

</manifest>
